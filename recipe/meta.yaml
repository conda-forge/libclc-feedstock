{% set version = "21.1.8" %}

package:
  name: libclc
  version: {{ version }}

source:
  - url: https://github.com/llvm/llvm-project/archive/refs/tags/llvmorg-{{ version.replace(".rc", "-rc") }}.tar.gz
    sha256: 7ba3f2a8d8fda88be18a31d011e8195d3b7f87f9fa92b20c94cba2d7f65b0e3f
    patches:
      - patches/0001-cross-compile-prepare_builtins.patch  # [build_platform != target_platform]

build:
  number: 2
  skip: True  # [win]

requirements:
  build:
    - {{ stdlib('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make    # [unix]
    - ninja   # [win]
    - llvmdev  {{ version }}.*                             # [build_platform != target_platform]
    - llvm-spirv {{ ".".join(version.split(".")[:2]) }}.*  # [build_platform != target_platform]
    - spirv-tools                                          # [build_platform != target_platform]
    - zlib                                                 # [build_platform != target_platform]
  host:
    - llvmdev  {{ version }}.*
    - llvm-spirv {{ ".".join(version.split(".")[:2]) }}.*
    - clang    {{ version }}.*
    - spirv-tools
    - zlib
  run_constrained:
    - llvmdev  {{ version }}.*

test:
  commands:
    - test -f ${PREFIX}/share/pkgconfig/libclc.pc  # [unix]
    - test -f ${PREFIX}/share/clc/nvptx64--.bc     # [unix]
    - test -f ${PREFIX}/share/clc/spirv64-mesa3d-.spv  # [unix]

about:
  home: https://llvm.org
  license: Apache-2.0
  license_file: LICENSE.TXT
  summary: |
    libclc is an open source, BSD licensed implementation of the library
    requirements of the OpenCL C programming language, as specified by the
    OpenCL 1.1 Specification.

extra:
  recipe-maintainers:
    - JarrettSJohnson
    - isuruf
