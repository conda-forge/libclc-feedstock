# On macOS, keep compiler version in sync with LLVM version to avoid conflicts
# between {{ compiler('cxx') }} and clang
c_compiler_version:
  - 20  # [osx]
cxx_compiler_version:
  - 20  # [osx]
spirv_tools:
  - 2024
